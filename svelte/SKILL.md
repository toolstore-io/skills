---
name: svelte
description: Sub-index for Svelte-specific frontend skills. Load this when doing Svelte frontend work, then load the specific skill it points to. For shared frontend skills (token-first styling, cva variants, primitive extraction, Storybook), see the frontend sub-index.
---

# Svelte Skills Sub-Index

Load the specific skill for your task. Shared frontend skills live in the `frontend` sub-index and apply here too.

| Skill | Load when you are... |
|---|---|
| `svelte-component` | Creating a new UI component or reorganizing an existing one |
| `bits-ui-composition` | Building interactive primitives (dialogs, menus, toggles, tabs, checkboxes) |
