---
name: react
description: Sub-index for React-specific frontend skills. Load this first when doing React frontend work, then load the specific skill it points to. Skip if the project uses Svelte or another framework.
---

# React Skills Sub-Index

Load the specific skill for your task. Also load the shared skills in the `frontend` sub-index — they apply to React as well.

| Skill | Load when you are... |
|---|---|
| `react-component` | Creating a new component or organizing an existing one |
| `radix-composition` | Building interactive primitives using Radix UI |
